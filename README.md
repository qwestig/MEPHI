# MEPHI — учебные проекты

## Структура
- `Logical programing/`
  - `lab1/` — Генеалогическое дерево (Prolog)

## Lab1: запуск
```bash
cd "Logical programing/lab1"
swipl -q -l tests.pl -t run_tests
swipl -q -l debug_backtracking.pl -g run_backtracking_debug -t halt
cat README.md
